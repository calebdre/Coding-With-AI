// connect 4 game in the chrome dev console
const rows = 6
const cols = 7
const board = []

// reset the game board to all '-' characters
const fillBoard = () => {}

// turn the board into a string and log it
const printBoard = () => {}

// check if a given column is full
const isColumnFull = (column) => {}

// add the chip character to the specified column, on the bottommost available row
const drop = (col, chip) => {}